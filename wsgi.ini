[uwsgi]
module = backend.wsgi:application

master = true
processes = 4
threads  = 2
enable-threads = true

http = :8000
vacuum = true            # clean up old sockets on shutdown
die-on-term = true
buffer-size = 65535
