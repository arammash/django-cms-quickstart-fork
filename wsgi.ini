[uwsgi]
module = backend.wsgi:application

master = true
processes = 4
threads  = 2
enable-threads = true

socket = :8000           # Render expects a socket on 8000, not an HTTP server on 80
vacuum = true            # clean up old sockets on shutdown
die-on-term = true
